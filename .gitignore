node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

.env
.env.local
.env.*.local

# このリポジトリはレビュー用にパブリック公開している。
# docs/ は社内向けの指示書・要件定義で、担当者名や営業判断が書かれているので出さない。
# 社内で共有するときは別途（プライベートリポジトリか、いままでどおりファイルで）。
docs/

.claude/settings.local.json

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
